{
  "target": "src/dashboard_template.html",
  "tests": "tests/test_type_and_spacing_scale.py",
  "cases": [
    {
      "id": "a-half-pixel-font-size-returns",
      "why": "the page title goes back to a hand-picked size off the scale -- how the page reached 21 distinct font sizes one reasonable-looking value at a time.",
      "find": ".page-head h2{font-family:'Plus Jakarta Sans', sans-serif; font-weight:800; font-size:var(--fs-32);",
      "replace": ".page-head h2{font-family:'Plus Jakarta Sans', sans-serif; font-weight:800; font-size:34px;",
      "expect_caught_by": "test_every_font_size_is_on_the_scale"
    },
    {
      "id": "an-off-grid-padding-returns",
      "why": "a card's padding is typed by hand again at a value off the 4px grid, which is how 232 literal spacing values accumulated while the spacing scale sat unused.",
      "find": ".matchup-header{font-family:'Plus Jakarta Sans', sans-serif; font-weight:700; font-size:var(--fs-16); margin-bottom:var(--s3);",
      "replace": ".matchup-header{font-family:'Plus Jakarta Sans', sans-serif; font-weight:700; font-size:var(--fs-16); margin-bottom:10px;",
      "expect_caught_by": "test_every_spacing_length_is_on_the_scale"
    },
    {
      "id": "a-literal-hides-beside-a-calc",
      "why": "the bottom nav's padding keeps its calc() for the safe area and gets a literal beside it -- the one shape the first pass of this migration skipped, because it left any value containing calc() alone.",
      "find": "padding:var(--s2) var(--s2) calc(8px + var(--safe-bottom));",
      "replace": "padding:8px 6px calc(8px + var(--safe-bottom));",
      "expect_caught_by": "test_every_spacing_length_is_on_the_scale"
    },
    {
      "id": "a-scale-step-that-does-not-exist",
      "why": "a rule asks for --fs-15, a step the scale does not have, so the browser drops the declaration and the element falls back to its inherited size with nothing in the console.",
      "find": ".at-symbol{color:var(--text-2); font-weight:500;}",
      "replace": ".at-symbol{color:var(--text-2); font-weight:500; font-size:var(--fs-15);}",
      "expect_caught_by": "test_every_scale_token_used_is_declared"
    }
  ]
}
