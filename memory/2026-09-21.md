# 2026-09-21

## Shipped

- **Queue item 1 recorded and closed**, straight to `main` at `294b9fe`. Both
  premises re-executed before any edit. The `#8A93A8` chevron is DEAD, not
  wrong in light mode; the no-script reader the two markup-visible selects
  were kept for cannot reach either page.
- **#79 opened**, the `<noscript>` note that fell out of the second premise,
  with `tests/test_noscript_note.py` and three mutation cases. Unaudited.

## What the re-execution measured

Playwright against the built page, served over localhost, vacuity guards
written before the first run.

- **Scripts on:** all four `.week-select` elements measure 1x1 with
  `clip-path: inset(50%)`, each navigated to through a real nav control so it
  was measured on its own active page. All four still carry the `8A93A8`
  background-image; none of them can paint it.
- **Scripts off:** one of nine sections active (`#page-ratings`), eighteen
  `[data-page]` controls all `<button>` with no href, no `:target` rule, no
  anchor reaching a page, no `<noscript>` at the time. The reachable page had
  one `<tr>` and 68 characters of visible text.
- **The five SVGs present with scripts off are icons**, not charts — week
  stepper arrows and the PDF link. The 2026-09-20 note said "no charts",
  which is right in substance; the selector was wider than the sentence.
- **The light theme is script-set.** Rendering under
  `prefers-color-scheme: light` with scripts off returns the dark page. So
  there is exactly one theme to check in a no-JS state, not two.

## Decided

- **The honest reason for shipping those selects visible is ORDERING, not
  reach.** The select cannot be hidden unless the control replacing it
  already exists, so a script dying in between leaves a working native
  select. Guards unchanged; only the recorded reasons were corrected.
- **Corrections went straight to `main`, the `<noscript>` went to a PR.** The
  first is documentation of a falsehood and the longer it sits the more it is
  inherited as true; the second changes the page.
- **The CLAUDE.md `Suite:` bump does NOT ride on #79.** A second commit there
  would need a description edit only Mark can make, and the stale figure is
  self-detecting — `session_start.py` reprints CLAUDE.md's number beside a
  real run. Recorded in `docs/context.md` instead. This is a deliberate
  departure from the #69 "put it on the branch" lesson, and the reason it is
  safe here is that nothing goes red and a mechanical check surfaces it.

## Surprised by

- **The correction's blast radius was eight sites, not the three proposed.**
  Two source comments the proposal never mentioned, a fourth template
  comment, three test docstrings with their assertion messages, a
  `test_week_stepper.py` docstring making the same claim about a different
  component, and three `why` fields rather than two. Found by a repo-wide
  search rather than the paths already in hand — the "conclusion reported as
  a search result" trap, avoided only because the search was run.
- **A false claim had propagated into a neighbouring component's docstring.**
  `test_week_stepper.py` called its select the no-script fallback. Same
  sentence, different component, and nothing connected them but the belief.
- **One `why` field also deferred to "Stage 11"**, a stage CLAUDE.md already
  records as never having existed. Two wrong things in one sentence, and only
  the colour claim was being looked for.
- **I nearly wrote a comment that my own next commit would falsify.** The
  corrected `enhanceSelect` comment said "no `<noscript>`" — true when
  written, false an hour later. Rephrased so a future note cannot make it
  wrong. The base that moves is sometimes your own branch.
- **`runner.py` honours only the LAST `--id`.** Three passed on one command
  ran one case and printed "All 1 mutations caught", which reads exactly like
  success. Caught only by reading the output instead of the exit. Every case
  was then run on its own.
- **`check_visual_claims_have_artifacts` prescribes a remedy it rejects.** Its
  failure message says to state the claim as a process note; the code has no
  process-note path at all. And `VISUAL_CLAIM_RE` fired on the word
  "screenshot" inside the disclaimer while missing "rendered ... and read on
  screen", so it failed an honest body and then passed the same body for the
  wrong reason. Too narrow and too wide at once. Queued, not fixed — one
  item, one PR.

## Housekeeping

- `Claude outputs/ctx_final.md` was last session's pre-edit draft of
  `docs/context.md`; the committed version is a superset. Moved out of the
  repository to `E:\nfl-scratch-20260921\` rather than deleted, so the
  working tree is clean and nothing was destroyed.
- `main` moved twice under this session from the agent-log collector. The
  first push was rejected because `session_start.py` reports ahead/behind
  against a remote ref it has not refreshed — the same stale-local-main shape
  CLAUDE.md records for `scout_preflight.py --base main`.

## Later the same session

- **#79 merged.** Booth's verdict block: 7 CONFIRMED, 0 discrepancies, 1
  UNVERIFIABLE, SAFE TO MERGE. Suite on `main` at `db79fc7` is 1286 passing,
  1 skipped — the predicted +13, and equal to Booth's own run. CLAUDE.md's
  `Suite:` line bumped in the follow-up commit, as `docs/context.md` said it
  must be.
- **Booth verified the half I could not.** The render claim came back
  UNVERIFIABLE for want of a browser, but it checked the MECHANISM by
  reading the source instead: `data-theme="light"` is set in exactly two
  places, both inside `<script>`, and no `@media (prefers-color-scheme:
  light)` rule exists. That is a better confirmation than the render was,
  and it is the pattern to ask for when a claim is visual — name the
  mechanism so the auditor has something to check without a browser.
- **The branch is deleted, local and remote.**

## Two defects in the audit itself

Neither changed the verdict, and that is what makes them worth recording.

- **The prose header disagreed with its own verdict block again** — header
  "Confirmed: 6, Unverifiable: 2", block 7 and 1. Tenth report. It cost
  nothing for the same reason it cost nothing on #78, zero discrepancies
  being zero under either count, and that is precisely why the open question
  of whether `cross_check()` should FAIL an audit rather than tally it has
  never been forced.
- **Booth quoted an untraceable figure.** Claim 4 said
  `test_every_anchor_still_matches_exactly_once` passes for "204 parametrized
  cases (`204 passed`)". Re-run here on the same commit: 203, with 38 case
  files; the whole module collects 619, so 204 is not a wider-scope number
  either. The untraceable-number defect, committed by the agent built to
  catch it, inside the claim doing the catching. **Check the figures inside
  an audit the way the audit checks the figures inside a body** — the
  conclusion being right is what makes a wrong number easy to relay.
