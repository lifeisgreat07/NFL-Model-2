# 2026-09-24

## Shipped

- **#95's fix held.** Every Booth run since posted a report. One on #96 shows
  "cancelled", which is the concurrency group replacing a run when a newer
  one starts, not a silent run.
- **#96 merged: the QB rating leak case study** and `src/measure_qb_leak.py`,
  which measured the leak's effect for the first time: every interval
  includes zero. Four audits. The first found the table did not reproduce on
  Linux; my fix blamed the platform, and the second audit's Linux run
  matched the Windows table, so that was wrong too. Final text reports all
  the runs and says the cause is unknown.
- **#97 merged: case study of Booth's first week.**
- **#98 opened: the rest of Stage 7** (Stage 2 corrections, regression suite
  write-up, lessons-learned page, architecture diagram, README rewrite with
  images), one commit per item.
- **Stage 7.6 done**: About panel description and topics, set in the
  signed-in browser.
- **The Booth regression suite ran for the first time** (`f7b4fff`), started
  by Mark: Booth caught the planted defect.

## Decided

- **Case studies live in the repo, with a short card on the page (Mark).**
  One copy of each story; the card links to it. Two full copies drift.
- **Incidents 2 and 3 folded into the QB-leak card (Mark).** Both were that
  afternoon.
- **The leak table stays, with its runs reported (Mark)**, rather than
  dropping the rows that moved between machines.
- **The rest of Stage 7 as one PR (Mark away).** Stacking five PRs would
  have broken the wait-for-Booth rule; one PR with one commit per item keeps
  each item separable.

## Surprised us

- **The leak table split two ways across four Linux audits**, two matching
  the Windows run and two agreeing on one pick fewer per model, with the same
  library versions on both sides. Cause unknown.
- **The regression suite had never run** in seventeen days, and main's one
  standing skip was that baseline check, not the HEAD-on-remote test
  CLAUDE.md explained the skip with.
- **Its first run found two loose ends**: an unplanned finding on the
  fixture's description, and a baseline recording a commit Booth never saw,
  because the record job re-assembles the throwaway repository.
- **The file bridge corrupted three PNGs** while reporting success. Now a
  trap entry.
- **The README's results table dated from the first upload** (2026-08-17),
  before the leak fix and the shrinkage revert.
- **The plan's "four of seven misdiagnosed" didn't survive counting**: two of
  the four were answers to an open question.
- **The weekly Claude routine's prompt is stale** (dropback-leader starter,
  dashboard.html, Tuesday). Not changed: it is Mark's scheduled task.

## Left open

- #98 waits on Booth, then Mark. Merging it finishes Stage 7.
- Stage 4 next, starting with the regression workflow's head and the stale
  routine prompt.

## Later the same day: Stage 1, #99, the routine, Stage 4 parked

- **Stage 1 retired** at Mark's request ("if we are not planning on doing
  anything in the future with it, we need to remove it"). Every item was
  re-checked first; the one live item moved to Stage 6 with a trigger.
- **#99 opened**: the regression record job now passes the audit job's head,
  the runner refuses a verdict about another commit (seven-character floor,
  same as `booth_report_posted.py`), the baseline's head was corrected by
  hand with evidence from run 36036544840's log, and the fixture's
  description says "One commit." to match what `assemble()` builds.
- **Claim 1 is still inferred.** Booth's report for the fixture run is only
  in the run's artifact. Mark was asked for permission to download it and
  had not answered by the time #99 opened, so the case study says "most
  likely".
- **The routine could not be edited by an agent** (it was created through
  the HTTP API). Mark pasted the new prompt, renamed it "Weekly QB override
  research", set `0 22 * * 1,3` and removed the unused `visualize`
  connector. Verified afterwards with list_triggers.
- **Stage 4 parked by Mark** ("cancel stage 4 work for this session") after
  data quality and the play-by-play cache were drafted and tested, and three
  more modules were drafted untested. Kept as a local branch, `stage4-draft`,
  on `markys`.

### Surprised us

- The file bridge refuses `.github/` and `.git/`. The workflow change went
  over as a patch and `git apply`.
- A full mutation run is now close to an hour, not six minutes, and a suite
  run alongside it failed on a mutation live at that moment. Killing a run
  mid-case left a mutated file behind twice. Both are trap entries now.
