# 2026-09-20

## Shipped

- **#78 opened.** Traces all six `ACCEPTED_CLOSE` entries that read
  "Not traced". Adds a Playwright verifier that renders the built page and
  prints which pages paint each meaning token, rewrites the six entries from
  that table, and guards the premise under the two "cannot meet" verdicts with
  four mutation cases. Second commit bumps the README's mutation-case count,
  which is forced because the guard counts files.
- **Three trap entries and a rewritten `docs/context.md`**, straight to `main`.

## What the trace found

- **Four of the six co-occur.** `--series-a`/`--series-d` on Season Accuracy;
  `--warn`/`--series-b` and `--good`/`--series-c` on Model Lab and Methodology.
  Every co-occurring instance carries a word or a shape, which is the ground
  on which `--good`/`--warn` is already accepted, so none is a new defect.
- **Two cannot meet.** `--accent-strong` has one consumer, `.srs-bar-fill`,
  on Power Ratings and Team Deep-Dive; `--series-d` has one, the picks entry
  of `SERIES`, on Season Accuracy.
- **Both KNOWN DEFECT entries also turned out to be pairs that never meet.**
  `--accent`/`--series-d` cannot collide the way its own entry describes: the
  pick tick and the My Picks series never share a page, and the only page
  painting both tokens does so via the sidebar brand mark. Left graded as they
  were; the grading is Mark's call and is the next action in `docs/context.md`.

## Decided

- **The verifier is standalone, not a suite test.** No CI job here has a
  browser and Playwright is not on `markys`. What the suite CAN check is the
  premise — each "cannot meet" token still has exactly one consumer, in the
  place the verdict names — so that is what was guarded. The co-occurrence
  findings themselves have nothing recomputing them, and that gap is written
  down in `docs/context.md` rather than papered over.
- **The README wording defect was NOT fixed on this branch.** Bumping the
  count is forced by the new case file; fixing "cases" to mean what the guard
  counts moves the guard's own regex, which is a second item. Recorded in the
  PR description so the choice is visible rather than missed.
- **The premise rule is a plain function checked over synthetic sources.** On
  today's template every branch but "the premise holds" is unreachable, which
  is exactly the arrangement that let two mutations survive on 2026-09-16.

## Surprised by

- **A sweep over live data answers for today's data.** The first sweep said
  `--series-d` appears on no page. True of a September browser with one graded
  week and no stored picks; false of the page. Three pairs would have been
  recorded as "never together" on the strength of it.
- **The legend does not need the data.** All four series swatches render as
  soon as a second week grades, picks or no picks — so `--series-a`/`--series-d`
  goes from unreachable to visible for every visitor on Tue 2026-09-22.
- **Two selectors that matched nothing**, each producing a tidy wrong verdict,
  each caught only by a vacuity guard written before the run.
- **A file-bridge commit reported success and wrote the old content**, twice,
  detectable only because a guard failed with a byte-identical message.
