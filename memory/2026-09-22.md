# 2026-09-22

## Shipped

- **#82 merged** (Week 3 QB context notes, opened by the Tuesday routine).
  Booth was SAFE TO MERGE; its title said 6 games against 5 in the body.
- **#83 merged**: a Booth run that posts no report now fails. It ran red on
  its own PR as predicted, because the action skips on a PR that edits its
  workflow.
- **#84 opened**: Stage 5, pre-registered in one commit and answered in the
  next. Nothing was accepted.
- **`announced-qb-capture` pushed, no PR yet**: each pick records the
  announced and model starters, and notes when they differ.

## Decided

- **Stage 5's rules were fixed before any result was seen.** Validation
  2022–23 screens, confirmation 2024–25 decides, ten slots, Bonferroni 99.5%,
  and 2026 is untouched as the forward test. The reason is that 2024–25 had
  already confirmed three earlier decisions, so it is not a fresh holdout. The
  budget sets how strict the bar is, not the number of questions that happen
  to get asked.
- **INCUMBENT stayed on the lagged QB after H1 came back INCONCLUSIVE**,
  exactly as registered, even though its point estimate was the largest in
  the family. Changing the incumbent because a result looked good is the
  thing the registry exists to stop.
- **The live model's QB input was NOT switched.** The capture branch records
  what was announced on each Tuesday instead, so the 2026 forward test can
  answer H1 on information that was really available. Switching on
  methodology grounds is left to Mark.

## Surprised us

- **Booth's first live run under #83 posted nothing (#84), in 5m08s.** That
  is inside the old "normal" duration range, so the #80 duration tell would
  have missed it. The step caught it.
- **Re-deriving the production QB starter matched on 1592 of 1599 games.**
  Two production calls break dropback ties differently. Parity came only from
  calling production's own functions.
- **The published backtest and the live pipeline answer different
  questions.** The backtest scores each game with that game's own
  most-dropbacks QB. Live uses last game's. Nothing had ever compared them,
  because both were "the QB rating".
- **The moneyline test first checked only symmetry**, and a mutation that
  broke the underdog formula survived it.
- **Desktop Commander dropped mid-session.** A background mutation run on
  `markys` kept going, and refreshing the MCP tools brought the tools back.

## Left open

- #84 needs a Booth re-run, and someone signed in to read why the first run
  posted nothing.
- Mark's decision on switching the live QB input.
- Open the `announced-qb-capture` PR after #84 merges, and rebase the README
  case count.

## Later the same session

### Shipped

- **#84 merged**: Stage 5. Booth's re-run gave 15 CONFIRMED, 1 UNVERIFIABLE (a
  claim about an uncommitted intermediate state), SAFE TO MERGE. The first run
  posted nothing, #83's step caught it, and the re-run passed that same step.
- **#85 merged**: v2.5. Each pick is now rated with the expected starter:
  a sourced override first, then the schedule's listed starter, then last
  game's QB with a note. Booth gave 12 CONFIRMED, 2 UNVERIFIABLE.
- **#86 merged**: the queued preflight/README guard gaps. Booth gave 14
  CONFIRMED and 1 DISCREPANCY: the body said "a merge from main" and there
  were two, because main had been merged in again after #85. It was
  description-only, so it merged as SAFE TO MERGE rather than spend another
  audit on the edit.
- **#87 opened**: Stage 9 (graded colours mean a scored pick, `--accent-strong`
  retired, Net Rating bar neutral).

### Decided

- **Mark switched the live QB input on methodology grounds** (v2.5), the same
  argument as weekly refit, even though H1 was INCONCLUSIVE. The 2026 forward
  test is what scores it.
- **The override rung exists because the schedule lags injury news.** On
  2026-09-22 it still listed Dart (NYG) and Daniels (WAS), both already ruled
  out.
- **Week 3's locked picks were not changed.** Predictions are write-once. A
  what-if run with Winston/Mariota overrides and listed starters flipped one
  pick (MIN@TB). That was reported in chat, not committed.
- **The Net Rating sign is not a hue.** The dataviz answer (a diverging pair)
  was rendered next to the neutral fill and rejected. It would be a fifth
  colour job, and its red would read as "wrong". Mark has the side-by-side
  as an artifact.
- **The button component moves to Stage 10.** The keyboard sweep found the
  focus pass already done.

### Surprised us

- **The weekly job locks picks on Tuesday at 11:00 UTC, before most injury
  news.** Overrides only help if they land before that run. When the run
  should lock is Mark's call, and it is queued.
- **`.tag-neutral` wore the accent.** The "neutral" pill, including the
  game card's toss-up tag, was painted in the colour of the model's lean.
  It was found only by looking at the rendered pills.
- **Removing the graded green made an older mutation survive.** The case
  that copied the old green onto the not-kept pill stopped being a collision
  once the green left, so it was re-aimed at the new outlined pill. A green
  suite would never have shown this.
- **Merging main into three open branches cost a README conflict every time.**
  The count moved on each merge. #86's wording fix ("case files") does not
  stop the number conflicting; only merging one branch at a time does.
- **#87's own body miscounted twice before preflight.** It said two commits
  and a merge (three and two) and "nine" uses above a list of seven.
  Preflight's scope check caught the unlisted commit; the other two were
  caught by reading the body against `git log`. Counts in prose are where
  every recent discrepancy has come from.
