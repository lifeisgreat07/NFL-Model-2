# 2026-09-24 (second session)

## Shipped

- **Stage 4, all nine items**, one PR at a time, each merged after Booth
  said SAFE TO MERGE with no discrepancies: #100 data-quality checks, #101
  Booth alerts, #102 nightly canary and schema snapshot, #103 build-output
  smoke check, #104 drift to an issue, #105 weekly summary and failure
  alert, #106 reproducibility audit and leak-free tests, #107 play-by-play
  cache for the canary.
- **The reproducibility audit reproduced every published backtest figure
  exactly** on markys, and Booth re-ran it on Linux on #106 and got the
  same.

## Decided

- **Scout may merge a PR itself when Booth says SAFE TO MERGE with no
  discrepancies** (Mark, for this run, while he was away). Anything else
  waits for a fix and a fresh audit.
- **The 272-game count is a WARNING, not an ERROR (Mark).** An odd count
  corrupts no single pick, and an ERROR on a locking run costs the week.
  The dry run then found 2022 lists 271.
- **The weekly summary is an Actions job summary, plus an issue when the run
  fails (Mark).**
- **The play-by-play cache is the canary's alone.** The run that makes
  picks always fetches fresh. The parked draft's comments had claimed cache
  settings in two workflows that did not exist.

## Surprised us

- **2022 has 271 regular-season games in nflverse**: the cancelled
  Buffalo-Cincinnati game is simply absent. The draft's ERROR would have
  stopped every run of a season like it.
- **A drift-step mutation survived** once the summary step was drafted after
  it: the test sliced to the commit step, and the new step's own
  `!cancelled()` satisfied it. Fixed before #104 opened. Trap entry added.
- **Booth caught #105 quoting a command shorter than the one run.** The
  bare command crashed on `Path(None)`. Fixed in a second commit; Mark
  pasted the corrected description. Trap entry added.
- **A refused write left a stale commit message**, which went up on an
  unaudited commit and was amended.
- **Booth's runner has nflverse network access**, contrary to CLAUDE.md's
  old line, which Booth itself then quoted on #107. Corrected.
- **Desktop Commander dropped out once**; RefreshMcpTools restored it.

## Left open

- The first nightly canary (06:00 UTC 2026-09-25) and first weekly summary
  (Tue 2026-09-29) have not run yet. The alerts have never fired.
- Stage 6 is next.
